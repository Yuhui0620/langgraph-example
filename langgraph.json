{
  "dependencies": ["./my_agent"],
  "graphs": {
    "agent": "./my_agent/agent.py:graph"
  },
  "env": ".env",
  "dockerfile_lines": [
    "RUN apt-get install -y make",
    "RUN . $HOME/.nvm/nvm.sh && npm install lodash"
  ]
}
